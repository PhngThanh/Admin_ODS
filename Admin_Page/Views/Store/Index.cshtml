@using Admin_Page.Models
@model AuthorizeCode
@{
    ViewBag.Title = "Authorized Code";
}

<h2>@ViewBag.Title</h2>

@*@Html.BeginForm("GenerateCode", "Store", new { ReturnUrl = Model.ReturnUrl }){

    }*@
@Html.ActionLink("Generate", "GenerateCode", "Store", new { @class = "btn btn-outline-secondary btn-lg rounded mx-auto d-block" })
@{
    if (Model == null)
    {
    }
    else
    {
    <p class="text-center mt-5 h1">
        @Model.code
    </p>
        
  
       
    }
}

@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/bootstrap")
