@model IEnumerable<Admin_Page.Models.AuthorizeCode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.createDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.expiredDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.createDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.expiredDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.codeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codeId })
        </td>
    </tr>
}

</table>
